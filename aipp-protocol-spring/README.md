# AIPP Protocol Spring

Spring integration for `aipp-protocol`: attach/registration support, host bindings and reusable
Spring-side protocol helpers. It contains no independently deployable service.

Build with `mvn test`; consumers use `org.example:aipp-protocol-spring:1.0-SNAPSHOT`.
