# Shared theme sources

Source definitions, compiler scripts, runtime interfaces and standard `.ones-theme` package
sources for the Ones theme system. `themes/` is the source of truth; generated CSS is written to
`../css/`, and distributable packages are written under `packages/`.

See `THEMES.md` for editing and generation commands. Do not edit generated CSS or packaged output
as if it were source.
