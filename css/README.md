# Shared AIPP CSS

Stable browser CSS consumed by the Ones host and AIPP widgets. Hand-maintained primitives and
shell rules live beside generated theme tokens and compiled theme overlays.

Theme source belongs in `../theme/`. Files marked `GENERATED` must be regenerated with the scripts
documented there rather than edited directly.
