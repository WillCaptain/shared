# Billing Contract

Pure Java Billing domain types and application ports shared by Billing-One and its callers. This
module contains no HTTP transport, Spring configuration, persistence, or executable service.

Build with `mvn test`; consumers use `org.example:billing-contract:1.0-SNAPSHOT`.
