# LLM Gateway Java SDK

Java HTTP/SSE SDK used by AIPPs to invoke the Ones host LLM Gateway. Wire types come from
`llm-gateway-contract`; protocol compatibility and golden fixtures live in
`llm-gateway-protocol`.

Build with `mvn test`; consumers use `org.example:llm-gateway-java-sdk:1.0-SNAPSHOT`.
